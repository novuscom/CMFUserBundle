{% extends 'NovuscomCMFBundle:Default:admin.html.twig' %}

{% block body -%}
    <div class="row">
        <div class="col col-md-6 col-xs-12">
            <div class="box">
                <div class="box-header">
                    <h3 class="box-title">Группа</h3>
                </div>
                <div class="box-body">
                    {{ form(edit_form) }}
                </div>
                <div class="box-footer">
                    <div class="row">
                        <div class="col col-md-6">
                            <a href="{{ path('admin_group') }}" class="btn btn-info">
                                <i class="glyphicon glyphicon-list"></i>
                                Список групп
                            </a>
                        </div>
                        <div class="col col-md-6">
                            <div class="pull-right">
                                {{ form(delete_form) }}
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>


{% endblock %}
